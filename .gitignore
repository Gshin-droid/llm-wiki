Thumbs.db
.DS_Store

# Разовые разрешения кнопки «всегда разрешать» — только локально.
# Сейчас файл прикрыт глобальным ~/.config/git/ignore, но тот не переживает
# клонирование: в свежем клоне (в том числе у облачной рутины) его нет.
.claude/settings.local.json
*.tmp
.claude/scheduled_tasks.lock
262588213843476.md

# Редакторские файлы Obsidian — только локально, не в репозиторий
.obsidian/

# Личные материалы — только локально, не в GitHub
/private/

# Артефакты Playwright MCP (снимки страниц, логи консоли) — только локально
.playwright-mcp/

# Черновики от дешёвых моделей (llm.py) — сырьё для проверки, в вики не публикуется
.llm-drafts/

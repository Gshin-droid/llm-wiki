# Memory tool (Claude API)

## Определение
Официальный, встроенный в Claude Messages API инструмент, который даёт агенту постоянную память между сессиями — директория файлов (`/memories`), которые Claude сам создаёт, читает, правит и удаляет через шесть команд (`view`, `create`, `str_replace`, `insert`, `delete`, `rename`). Работает **клиент-сайд**: Claude только запрашивает операцию, приложение разработчика её исполняет над своим собственным хранилищем — Anthropic данные не хранит. Источник: [[claude-memory-tool-docs]].

## Зачем нужен: just-in-time context retrieval
Вместо того чтобы тащить всю релевантную информацию в контекст заранее, агент записывает то, что узнал, в файлы и перечитывает их по мере надобности. Это держит активное контекстное окно сфокусированным на текущей задаче — критично для долгих сессий, которые иначе упираются в лимит токенов.

## Место среди других механизмов управления контекстом
- **Context editing** — клиент вычищает старые результаты инструментов.
- **Compaction** — сервер сам суммаризирует весь диалог при приближении к лимиту контекста.
- **Memory tool** — сохраняет то, что должно **пережить** суммаризацию (в отличие от первых двух, это не про уменьшение текущего контекста, а про перенос знания в следующую сессию).
Для долгоживущих агентов Anthropic рекомендует использовать все три вместе.

## Multi-session software development pattern
Практическая рекомендация для проектов, растянутых на много агентных сессий (прямое применение к вайбкодингу):
1. **Initializer-сессия** заранее готовит progress log, feature checklist и ссылку на init/startup-скрипт — до начала содержательной работы.
2. Каждая следующая сессия **сначала читает** эти файлы — восстанавливает состояние без повторного исследования кода.
3. Сессия **обновляет progress log перед завершением**.
4. Фича считается готовой только после **сквозной верификации**, что она реально работает — не когда код написан.

Официальная документация прямо отсылает к детальному кейс-стади этого паттерна на реальном репозитории — см. [[long-running-agent-harness]] (там тот же принцип реализован через git-коммиты + хуки, а не только через memory tool API).

## Безопасность — ответственность разработчика, не тула
Поскольку исполнение файловых операций целиком на стороне приложения:
- **Path traversal** — обязательная защита (`/memories/../../secrets.env` может выйти за пределы директории памяти); валидировать префикс, резолвить путь в каноническую форму, отклонять `../`/URL-encoded варианты.
- **Размер файлов** — ограничивать, давать Claude листать через `view_range` вместо одного гигантского файла.
- **Expiration** — периодически удалять давно неиспользуемую память.
- **Чувствительные данные** — Claude обычно сам отказывается их писать, но это не гарантия — нужна отдельная валидация.

## Дополнение (2026-07-30/31): auto-memory в самом Claude Code

То, что здесь описано как API-инструмент для собственного агента, в Claude Code стало поведением по умолчанию: статья Anthropic про контекст-инжиниринг поколения 5 ([[anthropic-context-engineering-claude-5]]) переводит ручную дописку `CLAUDE.md` через `#` в разряд прошлого подхода, а рабочим называет **auto-memory** — Claude сам сохраняет релевантное работе и пользователю, без явной просьбы.

**Механика (закрыто 2026-07-31 по официальной документации [[claude-code-memory-docs]], `code.claude.com/docs/en/memory`):**

- **Формат/место:** `~/.claude/projects/<project>/memory/` — индекс `MEMORY.md` (грузится в каждую сессию) + опциональные тематические файлы (грузятся по требованию, не при старте). `<project>` определяется по git-репозиторию — все worktree и поддиректории одного репо делят одну директорию.
- **Область видимости:** machine-local, per-repository — один уровень, не три (managed/user/project), как у `CLAUDE.md`. Между машинами и облачными окружениями не синхронизируется. У субагентов своя, отдельная от главного диалога, auto-memory (кроме `/fork`, наследующего родительский контекст).
- **Лимит:** в сессию грузятся только первые 200 строк/25KB `MEMORY.md`; `CLAUDE.md`-файлы этому лимиту не подчиняются, грузятся целиком.
- **Включение:** по умолчанию **on**; тумблер `/memory` или `autoMemoryEnabled` в settings.json; `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1`.
- **Конфликт с `CLAUDE.md`:** отдельного правила разрешения конфликта нет — оба источника «context, not enforced configuration», при противоречии Claude может выбрать любой. Единственный гарантированный способ — hook (`PreToolUse`), а не память.
- **Соотношение с этим API-тулом (memory tool):** официальная страница про auto-memory **вообще не упоминает** memory tool — это независимые механизмы, не одна фича под разными именами. Архитектурное сходство (индекс + тематические файлы, just-in-time подгрузка) есть, но реализации не связаны: memory tool — примитив API, хранилище держит приложение разработчика; auto-memory — встроенное поведение продукта Claude Code, хранилище фиксировано настройками CLI. См. разбор в [[claude-code-memory-docs]].

Практическое наблюдение из сессий этой вики совпадает с документацией: агент действительно ведёт директорию памяти с файлами-фактами и индексом-указателем, который грузится в начале сессии.

**Дополнение (2026-08-07, [[berezhnitsky-agent-memory-lies]]): изоляция субагентов — это ответ на конфликт писателей.** Отдельная auto-memory у каждого субагента (пункт «Область видимости» выше) записана в документации как факт устройства. Источник называет её тем, чем она является архитектурно: «один писатель на файл» — самый простой ответ на потерю записи при параллельной работе. Конфликтов ноль, потому что конфликтовать некому; цена — знание не переносится между агентами. Общая рамка, в которой это решение стоит рядом с альтернативами (append-only, якорь к источнику), — [[agent-memory-five-questions]], вопрос 3.

**Там же — утверждение, официальной документацией не подтверждённое (закрыто 2026-08-08, отрицательно).** Источник описывает auto-memory как петлю «агент пересказывает собственные записи, а поверх стоит фоновая консолидация, которая пересказы периодически пересобирает». Первые две части подтверждаются ([[claude-code-memory-docs]]: агент сам пишет заметки, в сессию грузятся первые 200 строк `MEMORY.md`), **третья не подтвердилась**. Допроверка (ежедневный процесс закрытия пробелов, `wiki/gaps-backlog.md`): повторный прямой фетч [[claude-code-memory-docs]] (`code.claude.com/docs/en/memory`, раздел "How it works") + построчная проверка официального `CHANGELOG.md` Claude Code на упоминания memory/consolidation/compaction дают согласованный отрицательный результат. Документация описывает синхронный цикл внутри самой сессии — «Claude reads and writes memory files during your session» — без отдельного фонового задания. Единственная процедура, похожая на «пересборку»: сразу после записи `MEMORY.md` Claude Code проверяет файл против лимита 200 строк/25KB и, если тот близок к порогу, тем же ходом просит Claude сократить индекс (перенести детали в тематические файлы, слить или выкинуть устаревшие записи) — это подсказка внутри текущего хода агента, а не отложенная фоновая работа. В `CHANGELOG.md` все найденные упоминания слова "memory" относятся либо к утечкам памяти процесса (2.1.200, 2.1.203, 2.1.215–2.1.217), либо к context compaction диалога — ни одной записи про auto-memory consolidation. Вывод: механизма, который источник описывает, в задокументированном виде нет; автор сам оговаривал «так было на момент записи» — расхождение, вероятно, из смешения с этим же внутрисессионным сокращением индекса или из устаревшего на момент записи наблюдения. Разбор общего наблюдения про пересказ, кормящий пересказ, как таковой остаётся в силе как общий принцип — см. [[agent-memory-five-questions]], вопрос 4 — просто применительно к Claude Code auto-memory этот конкретный механизм не подтверждён.

## Архитектурная параллель с этой вики
Это тот же базовый принцип, что и у [[persistent-wiki-pattern]] — директория markdown/текстовых файлов вместо векторной БД или прогона всей истории через контекст каждый раз. Разница — memory tool предназначен для **самостоятельного** ведения агентом своей рабочей памяти внутри одного продукта/сессии без куратора, а паттерн этой вики — человек-куратор явно направляет, что и как записывается, с более строгой структурой (entities/concepts/sources/projects/synthesis) и качественным контролем на входе (Ingest/Lint). Уточняет [[llm-memory-landscape]]: у Anthropic есть нативная альтернатива Mem0/Zep/Letta для тех, кто строит своего агента прямо на Claude API, а не берёт готовый memory-фреймворк.

## Источники
[[claude-memory-tool-docs]] (API-инструмент), [[claude-code-memory-docs]] (auto-memory в Claude Code), [[berezhnitsky-agent-memory-lies]] (изоляция субагентов как ответ на конфликт писателей)

## Связи
[[llm-memory-landscape]], [[persistent-wiki-pattern]], [[agent-memory-five-questions]], [[long-running-agent-harness]], [[claude-code]], [[claude-agent-sdk]], [[context-engineering-claude-5]]
